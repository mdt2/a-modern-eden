'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.sortByProp = undefined;

var _ramda = require('ramda');

var R = _interopRequireDefault(_ramda).default;

var _natsort = require('natsort');

var natsort = _interopRequireDefault(_natsort).default;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var sortObjects = natsort();

/**
 * Sort an array of objects by property.
 *
 * @param {String|Array} prop
 * A property to sort by. If passed as an array, it
 * will be treated as a deep property path.
 *
 * @param {Array} list
 * An array of objects to sort.
 *
 * @return {Array}
 * A sorted copy of the passed array.
 *
 * @example
 * sortByProp('order', items);
 * // [{order: 1}, {order: 2}]
 *
 * @example
 * sortByProp(['data', 'title'], items);
 * // [{data: {title: 'a'}}, {data: {title: 'b'}}]
 */
function sortByProp(prop, list) {
  var get = R.is(Array, prop) ? R.path : R.prop;
  return R.sort(function (elA, elB) {
    var a = get(prop, elA);
    var b = get(prop, elB);
    return sortObjects(a, b);
  }, list);
}

exports.sortByProp = sortByProp;